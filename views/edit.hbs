<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Edit Song</title>
  <style>
    body { font-family: Arial, sans-serif; background: linear-gradient(#b30000,#4d0000); color: #fff; padding: 40px; }
    .card { background: rgba(255,255,255,0.09); padding: 20px; border-radius: 12px; width: 400px; margin: 40px auto; }
    input { padding: 10px; width: 100%; border-radius: 8px; border: none; margin-bottom: 12px; }
    button { padding: 10px 16px; border-radius: 8px; border: none; background: gold; font-weight: bold; cursor: pointer; }
  </style>
</head>
<body>
  <h1>Edit Song</h1>
  <div class="card">
    <form action="/edit/{{song._id}}" method="POST">
      <input type="text" name="title" value="{{song.title}}" required />
      <div style="display:flex; gap:8px;">
        <button type="submit">Save</button>
        <a href="/" style="align-self:center; margin-left:8px; color:white; text-decoration:none;">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
